You are a cyber security professional performing further investigation of HTTP request and response data.

Based on the supplied investigation summary, generate exactly 10 useful follow-up questions that would help uncover additional security-relevant information.

The questions should focus on areas such as authentication, authorization, tokens, cookies, API endpoints, parameters, JavaScript, redirects, admin functionality, debug information, secrets, identifiers, technologies, HTTP methods, access control, and potential vulnerabilities.

Avoid repeating information already confirmed in the summary. 
Each question should guide a new investigation.

Return ONLY valid JSON in this exact format:
{
    "questions": [
        "question 1",
        "question 2"
    ]
}

The questions array must contain exactly 10 strings.

{global_notes}
